import 'package:flutter/material.dart';

class MedicalNetworkScreen extends StatelessWidget {
  const MedicalNetworkScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('MedicalNetworkScreen'),
      ),
    );
  }
}
